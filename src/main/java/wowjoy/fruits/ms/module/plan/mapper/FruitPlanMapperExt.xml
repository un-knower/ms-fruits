<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wowjoy.fruits.ms.module.plan.mapper.FruitPlanMapperExt">
    <resultMap id="BaseResultMap" type="wowjoy.fruits.ms.module.plan.FruitPlanDao">
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="plan_status" jdbcType="VARCHAR" property="planStatus"/>
        <result column="percent" jdbcType="INTEGER" property="percent"/>
        <result column="estimated_start_date" jdbcType="TIMESTAMP" property="estimatedStartDate"/>
        <result column="estimated_end_date" jdbcType="TIMESTAMP" property="estimatedEndDate"/>
        <result column="start_date" jdbcType="TIMESTAMP" property="startDate"/>
        <result column="end_date" jdbcType="TIMESTAMP" property="endDate"/>
        <result column="parent_id" jdbcType="VARCHAR" property="parentId"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
        <result column="task_id" jdbcType="VARCHAR" property="taskId"/>
        <collection property="logs" resultMap="LogsResultMap" columnPrefix="flogs_"/>
        <collection property="users" ofType="wowjoy.fruits.ms.module.user.FruitUserDao" resultMap="UserResultMap"
                    columnPrefix="user_"/>
        <collection property="tasks" resultMap="TaskResultMap" columnPrefix="task_"/>
    </resultMap>
    <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="wowjoy.fruits.ms.module.plan.FruitPlanDao">
        <result column="description" jdbcType="LONGVARCHAR" property="description"/>
    </resultMap>
    <resultMap id="UserResultMap" type="wowjoy.fruits.ms.module.user.FruitUserDao">
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="birthday" jdbcType="TIMESTAMP" property="birthday"/>
        <result column="user_email" jdbcType="VARCHAR" property="userEmail"/>
        <result column="user_sex" jdbcType="VARCHAR" property="userSex"/>
        <result column="job_title" jdbcType="VARCHAR" property="jobTitle"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
        <!-- 扩展字段 -->
        <result column="pu_role" jdbcType="VARCHAR" property="planRole"/>
    </resultMap>
    <resultMap id="LogsResultMap" type="wowjoy.fruits.ms.module.logs.FruitLogsDao">
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="fruit_uuid" jdbcType="VARCHAR" property="fruitUuid"/>
        <result column="fruit_type" jdbcType="VARCHAR" property="fruitType"/>
        <result column="json_object" jdbcType="LONGVARCHAR" property="jsonObject"/>
        <result column="operate_type" jdbcType="VARCHAR" property="operateType"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
    </resultMap>
    <resultMap id="TaskResultMap" type="wowjoy.fruits.ms.module.task.FruitTaskDao">
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="task_status" jdbcType="VARCHAR" property="taskStatus"/>
        <result column="status_description" jdbcType="VARCHAR" property="statusDescription"/>
        <result column="task_level" jdbcType="VARCHAR" property="taskLevel"/>
        <result column="estimated_end_date" jdbcType="DATE" property="estimatedEndDate"/>
        <result column="end_date" jdbcType="DATE" property="endDate"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
    </resultMap>
    <sql id="Example_Where_Clause">
        <foreach collection="example.oredCriteria" item="criteria" separator="or">
            <if test="criteria.valid">
                <trim prefix="(" prefixOverrides="and" suffix=")">
                    <foreach collection="criteria.criteria" item="criterion">
                        <choose>
                            <when test="criterion.noValue">
                                and ${replace}.${criterion.condition}
                            </when>
                            <when test="criterion.singleValue">
                                and ${replace}.${criterion.condition} #{criterion.value}
                            </when>
                            <when test="criterion.betweenValue">
                                and ${replace}.${criterion.condition} #{criterion.value} and
                                #{criterion.secondValue}
                            </when>
                            <when test="criterion.listValue">
                                and ${replace}.${criterion.condition}
                                <foreach close=")" collection="criterion.value" item="listItem" open="("
                                         separator=",">
                                    #{listItem}
                                </foreach>
                            </when>
                        </choose>
                    </foreach>
                </trim>
            </if>
        </foreach>
    </sql>
    <sql id="Base_Column_List">
    ${replace}.uuid,
    ${replace}.title,
    ${replace}.plan_status,
    ${replace}.percent,
    ${replace}.start_date,
    ${replace}.end_date,
    ${replace}.estimated_start_date,
    ${replace}.estimated_end_date,
    ${replace}.parent_id,
    ${replace}.modify_date_time,
    ${replace}.create_date_time,
    ${replace}.is_deleted,
    ${replace}.description
  </sql>
    <sql id="User_Column_List">
    ${replace}.uuid as ${prefix}uuid,
    ${replace}.user_id as ${prefix}user_id,
    ${replace}.user_name as ${prefix}user_name,
    ${replace}.birthday as ${prefix}birthday,
    ${replace}.user_email as ${prefix}user_email,
    ${replace}.user_sex as ${prefix}user_sex,
    ${replace}.job_title as ${prefix}job_title,
    ${replace}.description as ${prefix}description,
    ${replace}.modify_date_time as ${prefix}modify_date_time,
    ${replace}.create_date_time as ${prefix}create_date_time,
    ${replace}.is_deleted as ${prefix}is_deleted
  </sql>
    <sql id="user_Base_Column_List">
        uuid, user_id, user_name, birthday, user_email, user_sex, job_title, description,
        modify_date_time, create_date_time, is_deleted
    </sql>
    <sql id="Logs_Column_List">
        ${replace}.uuid as ${prefix}uuid,
        ${replace}.user_id as ${prefix}user_id,
        ${replace}.fruit_uuid as ${prefix}fruit_uuid,
        ${replace}.fruit_type as ${prefix}fruit_type,
        ${replace}.json_object as ${prefix}json_object,
        ${replace}.operate_type as ${prefix}operate_type,
        ${replace}.modify_date_time as ${prefix}modify_date_time,
        ${replace}.create_date_time as ${prefix}create_date_time,
        ${replace}.is_deleted as ${prefix}is_deleted
    </sql>
    <sql id="Task_Column_List">
        ${replace}.uuid as ${prefix}uuid,
        ${replace}.title as ${prefix}title,
        ${replace}.task_status as ${prefix}task_status,
        ${replace}.status_description as ${prefix}status_description,
        ${replace}.task_level as ${prefix}task_level,
        ${replace}.estimated_end_date as ${prefix}estimated_end_date,
        ${replace}.end_date as ${prefix}end_date,
        ${replace}.description as ${prefix}description,
        ${replace}.modify_date_time as ${prefix}modify_date_time,
        ${replace}.create_date_time as ${prefix}create_date_time,
        ${replace}.is_deleted as ${prefix}is_deleted
    </sql>
    <sql id="Task_Base_Column_List">
        uuid, title, task_status, status_description, task_level, estimated_end_date, end_date,
        description, modify_date_time, create_date_time, is_deleted
    </sql>
    <select id="selectByProjectId" parameterType="wowjoy.fruits.ms.module.plan.example.FruitPlanExample"
            resultMap="ResultMapWithBLOBs">
        select
        <include refid="Base_Column_List">
            <property name="replace" value="plan"/>
        </include>
        from fruits_plan plan
        inner join (
        select plan_id from plan_project_relation where
        project_id = #{projectId}
        and is_deleted = 'N'
        ) ppr on ppr.plan_id = plan.uuid
        <if test="example != null">
            <trim suffixOverrides="AND | OR" prefixOverrides="AND | OR">
                <where>
                    <include refid="Example_Where_Clause">
                        <property name="replace" value="plan"/>
                    </include>
                </where>
            </trim>
        </if>
        <if test="example.orderByClause != null">
            order by plan.${example.orderByClause}
        </if>
    </select>

    <select id="selectUserByPlanIds" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List">
            <property name="replace" value="plan"/>
        </include>
        ,
        <include refid="User_Column_List">
            <property name="replace" value="fruitUser"/>
            <property name="prefix" value="user_"/>
        </include>
        from fruits_plan plan
        inner join (
        select plan_id,user_id from plan_user_relation
        where
        plan_id in
        <foreach collection="planIds" item="id" close=")" open="(" separator=",">
            #{id}
        </foreach>
        AND
        is_deleted = 'N'
        ) userRelation on userRelation.plan_id = plan.uuid
        RIGHT join (
        select
        <include refid="user_Base_Column_List"/>
        from fruits_user where is_deleted = 'N'
        ) fruitUser on fruitUser.user_id = userRelation.user_id
        <trim prefixOverrides="AND | OR" suffixOverrides="AND | OR">
            <where>
                plan.is_deleted = 'N'
            </where>
        </trim>
        order by fruitUser.user_id = #{userId} desc
    </select>

    <select id="selectTaskByPlanIds" resultMap="BaseResultMap">
        select
        plan.uuid
        ,
        <include refid="Task_Column_List">
            <property name="replace" value="task"/>
            <property name="prefix" value="task_"/>
        </include>
        from fruits_plan plan
        inner join (
        select task_id,plan_id from task_plan_relation
        where is_deleted = 'N'
        and plan_id in
        <foreach collection="planIds" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        ) taskPlanRelation on taskPlanRelation.plan_id = plan.uuid
        left join (select
        <include refid="Task_Base_Column_List"/>
        from fruits_task) task on task.uuid = taskPlanRelation.task_id

    </select>
</mapper>