<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wowjoy.fruits.ms.module.notepad.mapper.FruitNotepadMapperExt">
    <resultMap id="BaseResultMap" type="wowjoy.fruits.ms.module.notepad.FruitNotepadDao">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 04 11:25:27 CST 2017.
        -->
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="content" jdbcType="VARCHAR" property="content"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="state" jdbcType="VARCHAR" property="state"/>
        <result column="estimated_submit_date" jdbcType="TIMESTAMP" property="estimatedSubmitDate"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
        <association property="user" resultMap="UserResultMap" columnPrefix="fuser_"/>
        <collection property="logs" ofType="wowjoy.fruits.ms.module.logs.FruitLogsDao" resultMap="LogsResultMap"
                    columnPrefix="flogs_"/>
    </resultMap>
    <resultMap id="UserResultMap" type="wowjoy.fruits.ms.module.user.FruitUserDao">
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="birthday" jdbcType="TIMESTAMP" property="birthday"/>
        <result column="user_email" jdbcType="VARCHAR" property="userEmail"/>
        <result column="user_sex" jdbcType="VARCHAR" property="userSex"/>
        <result column="job_title" jdbcType="VARCHAR" property="jobTitle"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
        <!-- 扩展字段 -->
        <result column="pu_role" jdbcType="VARCHAR" property="planRole"/>
    </resultMap>
    <resultMap id="LogsResultMap" type="wowjoy.fruits.ms.module.logs.FruitLogsDao">
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="fruit_uuid" jdbcType="VARCHAR" property="fruitUuid"/>
        <result column="fruit_type" jdbcType="VARCHAR" property="fruitType"/>
        <result column="json_object" jdbcType="LONGVARCHAR" property="jsonObject" />
        <result column="operate_type" jdbcType="VARCHAR" property="operateType"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
    </resultMap>
    <sql id="Example_Where_Clause">
        <foreach collection="example.oredCriteria" item="criteria" separator="or">
            <if test="criteria.valid">
                <trim prefix="(" prefixOverrides="and" suffix=")">
                    <foreach collection="criteria.criteria" item="criterion">
                        <choose>
                            <when test="criterion.noValue">
                                and ${replace}.${criterion.condition}
                            </when>
                            <when test="criterion.singleValue">
                                and ${replace}.${criterion.condition} #{criterion.value}
                            </when>
                            <when test="criterion.betweenValue">
                                and ${replace}.${criterion.condition} #{criterion.value} and
                                #{criterion.secondValue}
                            </when>
                            <when test="criterion.listValue">
                                and ${replace}.${criterion.condition}
                                <foreach close=")" collection="criterion.value" item="listItem" open="("
                                         separator=",">
                                    #{listItem}
                                </foreach>
                            </when>
                        </choose>
                    </foreach>
                </trim>
            </if>
        </foreach>
    </sql>

    <sql id="Base_Column_List">
        ${replace}.uuid,
        ${replace}.content,
        ${replace}.user_id,
        ${replace}.state,
        ${replace}.estimated_submit_date,
        ${replace}.description,
        ${replace}.modify_date_time,
        ${replace}.create_date_time,
        ${replace}.is_deleted
    </sql>
    <sql id="User_Column_List">
        ${replace}.uuid as ${prefix}uuid,
        ${replace}.user_id as ${prefix}user_id,
        ${replace}.user_name as ${prefix}user_name,
        ${replace}.birthday as ${prefix}birthday,
        ${replace}.user_email as ${prefix}user_email,
        ${replace}.user_sex as ${prefix}user_sex,
        ${replace}.job_title as ${prefix}job_title,
        ${replace}.description as ${prefix}description,
        ${replace}.modify_date_time as ${prefix}modify_date_time,
        ${replace}.create_date_time as ${prefix}create_date_time,
        ${replace}.is_deleted as ${prefix}is_deleted
     </sql>
    <sql id="Logs_Column_List">
        ${replace}.uuid as ${prefix}uuid,
        ${replace}.user_id as ${prefix}user_id,
        ${replace}.fruit_uuid as ${prefix}fruit_uuid,
        ${replace}.fruit_type as ${prefix}fruit_type,
        ${replace}.json_object as ${prefix}json_object,
        ${replace}.operate_type as ${prefix}operate_type,
        ${replace}.modify_date_time as ${prefix}modify_date_time,
        ${replace}.create_date_time as ${prefix}create_date_time,
        ${replace}.is_deleted as ${prefix}is_deleted
    </sql>
    <select id="selectByCurrentUser" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List">
            <property name="replace" value="notepad"/>
        </include>
        FROM
        fruits_notepad notepad
        left join fruits_user fuser on fuser.user_id = notepad.user_id
        <trim suffixOverrides="AND | OR" prefixOverrides="AND | OR">
            <where>
                <include refid="Example_Where_Clause">
                    <property name="replace" value="notepad"/>
                </include>
                AND fuser.is_deleted = 'N'
            </where>
        </trim>
        <if test="example.orderByClause != null">
            order by ${example.orderByClause}
        </if>
    </select>

    <select id="selectByTeamIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List">
            <property name="replace" value="notepad"/>
        </include>
        FROM
        fruits_notepad notepad
        left join fruits_user fuser on fuser.user_id = notepad.user_id
        left join user_team_relation utr on utr.user_id = fuser.user_id
        <trim suffixOverrides="AND | OR" prefixOverrides="AND | OR">
            <where>
                <include refid="Example_Where_Clause">
                    <property name="replace" value="notepad"/>
                </include>
                AND utr.team_id IN
                <foreach collection="teamIds" item="id" open="(" close=")" separator=",">
                    #{id}
                </foreach>
            </where>
        </trim>
        <if test="example.orderByClause != null">
            order by ${example.orderByClause}
        </if>
    </select>

    <select id="selectJoinLogsByNotepadIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List">
            <property name="replace" value="notepad"/>
        </include>
        ,
        <include refid="Logs_Column_List">
            <property name="replace" value="flogs"/>
            <property name="prefix" value="flogs_"/>
        </include>
        FROM
        fruits_notepad notepad
        right join
        (select uuid, user_id, fruit_uuid, json_object, fruit_type, operate_type, modify_date_time, create_date_time,
        is_deleted from fruits_logs where fruit_type = 'NOTEPAD' and is_deleted = 'N') flogs on flogs.fruit_uuid =
        notepad.uuid
        <trim suffixOverrides="AND | OR" prefixOverrides="AND | OR">
            <where>
                notepad.uuid in
                <foreach collection="notepadIds" close=")" open="(" item="id" separator=",">
                    #{id}
                </foreach>
            </where>
        </trim>
        ORDER BY flogs.create_date_time desc
    </select>

    <select id="selectJoinUserByNotepadIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List">
            <property name="replace" value="notepad"/>
        </include>
        ,
        <include refid="User_Column_List">
            <property name="replace" value="fuser"/>
            <property name="prefix" value="fuser_"/>
        </include>
        FROM
        fruits_notepad notepad
        right join fruits_user fuser on fuser.user_id = notepad.user_id
        <trim suffixOverrides="AND | OR" prefixOverrides="AND | OR">
            <where>
                notepad.uuid in
                <foreach collection="notepadIds" close=")" open="(" item="id" separator=",">
                    #{id}
                </foreach>
                and fuser.is_deleted = 'N'
            </where>
        </trim>
    </select>
</mapper>