<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="wowjoy.fruits.ms.module.notepad.mapper.FruitNotepadMapperExt">
    <resultMap id="BaseResultMap" type="wowjoy.fruits.ms.module.notepad.FruitNotepadDao">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Dec 04 11:25:27 CST 2017.
        -->
        <result column="uuid" jdbcType="VARCHAR" property="uuid"/>
        <result column="content" jdbcType="LONGVARCHAR" property="content"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="estimated_submit_date" jdbcType="TIMESTAMP" property="estimatedSubmitDate"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="modify_date_time" jdbcType="TIMESTAMP" property="modifyDateTime"/>
        <result column="create_date_time" jdbcType="TIMESTAMP" property="createDateTime"/>
        <result column="is_deleted" jdbcType="VARCHAR" property="isDeleted"/>
    </resultMap>
    <sql id="Example_Where_Clause">
        <foreach collection="example.oredCriteria" item="criteria" separator="or">
            <if test="criteria.valid">
                <trim prefix="(" prefixOverrides="and" suffix=")">
                    <foreach collection="criteria.criteria" item="criterion">
                        <choose>
                            <when test="criterion.noValue">
                                and ${replace}.${criterion.condition}
                            </when>
                            <when test="criterion.singleValue">
                                and ${replace}.${criterion.condition} #{criterion.value}
                            </when>
                            <when test="criterion.betweenValue">
                                and ${replace}.${criterion.condition} #{criterion.value} and
                                #{criterion.secondValue}
                            </when>
                            <when test="criterion.listValue">
                                and ${replace}.${criterion.condition}
                                <foreach close=")" collection="criterion.value" item="listItem" open="("
                                         separator=",">
                                    #{listItem}
                                </foreach>
                            </when>
                        </choose>
                    </foreach>
                </trim>
            </if>
        </foreach>
    </sql>

    <sql id="Base_Column_List">
        ${replace}.uuid,
        ${replace}.content,
        ${replace}.user_id,
        ${replace}.estimated_submit_date,
        ${replace}.description,
        ${replace}.modify_date_time,
        ${replace}.create_date_time,
        ${replace}.is_deleted
    </sql>

    <select id="selectByTeamIds" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List">
            <property name="replace" value="notepad"/>
        </include>
        FROM
        fruits_notepad notepad
        left join fruits_user fuser on fuser.user_id = notepad.user_id
        left join user_team_relation utr on utr.user_id = fuser.user_id
        <trim suffixOverrides="AND | OR" prefixOverrides="AND | OR">
            <where>
                <include refid="Example_Where_Clause">
                    <property name="replace" value="notepad"/>
                </include>
                AND utr.team_id IN
                <foreach collection="teamIds" item="id" open="(" close=")" separator=",">
                    #{id}
                </foreach>
            </where>
        </trim>
        <if test="example.orderByClause != null">
            order by ${example.orderByClause}
        </if>
    </select>

    <select id="selectByCustom" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List">
            <property name="replace" value="notepad"/>
        </include>
        FROM
        fruits_notepad notepad
        <trim suffixOverrides="AND | OR" prefixOverrides="AND | OR">
            <where>
                <include refid="Example_Where_Clause">
                    <property name="replace" value="notepad"/>
                </include>
                AND
                <foreach collection="customs" item="custom">
                    ${custom}
                </foreach>
            </where>
        </trim>
        <if test="example.orderByClause != null">
            order by ${example.orderByClause}
        </if>
    </select>
</mapper>